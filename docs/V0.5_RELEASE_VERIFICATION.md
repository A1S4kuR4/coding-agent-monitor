# v0.5.0 release verification

> **LOCAL CANDIDATE — verified 2026-09-23; publication gates remain open.**
> A result is `NOT RUN` until its own evidence lands here. Follow the v0.4.0
> discipline: record the exact build-source SHA with the package, keep failures
> as separate retained attempts, never relabel a historical waiver as a pass,
> and do not publish while gates remain open.

Date: 2026-09-23. The first frontend rebuild, Windows gates and walkthrough
below used working tree at `a6e1bdbd6924b53e3db469ccffe98a30a512cb2b`.
The clean final MSI/NSIS build and native acceptance use **build-source commit
`2927799940b9d37e4e84707e0b4837368fe00add`** (detached worktree). Later
documentation commits do not change that binary source identity.

## Source and fixes (expected contents)

- `6d00817`: control-language / trend-readability style pass that predates
  the main-screen refactor.
- `dfe8690`: P0 main-screen refactor — trend chrome consolidation, today
  composition strip, today pinned by default (review A1/B1/C1).
- `45ba1a2`: P1 marks & controls language — sigils, underline tabs, cost
  demotion, 30-day week aggregation (marks §2–3, review A2/B2).
- `c46aac2`: P2 chart label hierarchy and refresh-presence dot (review
  A4/C3).
- `d3f99f2`: Claude Desktop collector backend (`claude_desktop.rs`, discovery,
  registry, production wiring and tests), bringing local collection to 18 agents.
- `d82f6d3`: D4 statistics-explainer copy distinguishes no usage, unknown
  prices and a complete zero-cost estimate; English E2E assertion.
- `2927799`: gate the test-only worker override import in release compilation.
- The 0.5.0 version bump predates this cycle; the final package manifests and
  Cargo package already report 0.5.0.

## Prerequisites before packaging (fill in Windows)

| Check | Current result |
| --- | --- |
| cargo fmt -- --check / strict Clippy / Cargo check (incl. Claude Desktop backend) | **PASS (final source)** — initial fmt attempt found two test-layout diffs, fixed before `d3f99f2`; `cargo fmt -- --check`, `cargo check` and `cargo clippy --all-targets -- -D warnings` then passed, including a post-`2927799` rerun. Release `--lib --bins` strict Clippy passed too. A separate release `--all-targets` attempt failed because existing integration tests call a debug-only worker override; this is retained below, not counted as a pass. |
| Cargo test (incl. new Claude Desktop suites) | **PASS (final source): 208 tests / 0 failures** across library and integration groups; rerun after `2927799`. Optional external shadow/performance wrapper tests that return early are not claims of completed external audits. |
| pnpm lint / typecheck | **PASS (final clean source)** — both passed after frozen install; prior 2026-09-23 working-tree pass was 48.5s / 5.1s. |
| pnpm test | **PASS (final clean source): 16 files / 152 tests**; same count as the earlier Windows and sandbox runs. |
| pnpm test:e2e | **PASS (final clean source): 47/47** on system Edge (`channel: "msedge"`); a focused D4 English explainer rerun also passed after the last wording tweak. |
| pnpm vendor:verify | **PASS (final clean source)** — vendored tree, patches, pricing and linkage verified; `minreq` / `libsqlite3-sys` absence probes are expected output. |
| pnpm build | **PASS (final clean source)** — Vite 8.2.2 emitted `index-VxwDC10a.js` / `index-ANW7SaGh.css`; the Tauri build reran the same frontend gate. |
| Manual UI walkthrough: 680x700 default window, 125%/150% DPI, 200% text scaling, reduced motion, composition strip, pinned-today panel, filter tabs keyboard path, week↔day switch | **PASS for the listed current-host items after D4 remediation** — historical first-pass D4 failure and E2/E3/E4/E6 proxy limits remain itemized below; native supplement appears later. |

## Frontend rebuild evidence (2026-09-23)

- Prior `dist/` (2026-09-20 22:02) predated commit `0343b19` (2026-09-20
  22:06) by timestamp only. The rebuild produced **byte-identical asset
  hashes** (`index-BNp1VEJY.js`, `index-DbiAxTvS.css`), proving the 22:02
  build had been made from a working tree that already contained both fixes;
  the 2026-09-23 rebuild (11.9s) removes the ambiguity.
- Sigil path marker `M8 6.7` present in `dist/assets/index-*.js` (grep hit).
- `0343b19` CSS hierarchy markers present in `dist/assets/index-*.css`:
  `.history-models>div>p{...font-variant-numeric:tabular-nums}` and
  `.history-models summary{cursor:pointer;font-weight:500;color:var(--ink)}`.
- `dist/` is untracked; build artifacts not committed.

## Manual walkthrough (2026-09-23)

Method: the existing `src-tauri/target/release/coding-agent-monitor.exe`
(built 2026-09-20 22:03 with the 22:02 frontend assets) ran on Windows 11 x64
against an **isolated synthetic profile** with Claude and Codex session data.
The 2026-09-23 `pnpm build` produced byte-identical frontend asset hashes; the
EXE was **not rebuilt**, so uncommitted collector work was not incorporated.
WebView2 was driven through CDP `:9224` at the native host's 125% DPI
(`window.devicePixelRatio=1.25`). Screenshots are ignored local evidence under
`.scaffold-root/v05-verify/shots/`: `01–12` were recaptured on 2026-09-23;
`20–32` cover the expanded cases. The isolated records and every screenshot
are synthetic evidence, never actual user usage.

Raw driver outputs were retained in `.scaffold-root/v05-verify/`:
`driver-wrapup-2026-09-23.log` (first attempt stopped at the absent Codex tab
because the original seed contained only Claude),
`driver-wrapup-2026-09-23-attempt2.log` through `-attempt7.log`, and
`driver-2026-09-23.log` plus `-attempt2.log`. The final wrap-up attempt had
**one FAIL (D4)** and explicitly reported E6 **NOT RUN**. The core driver had
**one assertion FAIL**: computed `transition-duration=1e-05s`, numerically
equal to its expected `0.01ms`; the reduced-motion rule also passed a direct
animation-iteration probe in the wrap-up driver. Earlier wrap-up FAILs were
harness assumptions corrected and retained in those logs: absent Codex seed,
125% DPI border rounding (`1.6px` CSS computes to 2 device pixels), selecting
the date child instead of the full week panel, counting bar tracks as colored
segments, probing the historical panel for today's short cost mark, probing
the wrong explainer, and sampling a brief refresh after it had settled.

| Item | Result | Evidence / limit |
| --- | --- | --- |
| A1 | PASS | `01`: 680×700 first viewport shows the seven bars with the core summary and controls. |
| A2 | PASS | `01`, `10`: today has the selected backplate/ring and stronger number. |
| A3 | PASS | `10`, `21`, `30`: pinned panel contains date, total, agents, delta, cost, coverage, composition, model disclosures and fixed close. Content below the fold is naturally scrollable. |
| A4 | PASS | `01`: Agent composition active, 5px segmented strip and text action. |
| A5 | PASS | `03`: type tab and gray-scale token-type segments. |
| A6 | PASS | `02–03`: detail rows expand; action changes to collapse and closes again. |
| A7 | PASS | `01`, `04`: colored agent-share slivers scale with today's shares. |
| A8 | PASS | Core driver closes panel; wrap-up keyboard close returned focus to today's bar. |
| A9 | PASS | `21`: close then click/Enter today to pin again. |
| B1 | PASS | `05`: plain text/sigil filter tabs without chip backgrounds. |
| B2 | PASS | Wrap-up hover probe: Codex text becomes ink-2 (`rgb(179, 169, 155)`), with no background; `22`. |
| B3 | PASS | `22`: Codex pressed, agent-color underline, scope hint changes while today's total stays complete. |
| B4 | PASS | Wrap-up: All pressed, ink underline and scaling hint restored. |
| B5 | PASS | `23`: Tab reaches filter, focus ring; Enter selects, Space toggles back and Escape clears pinned bar. |
| C1 | PASS | `08`: 30 days defaults to five scroll-free week bars and exposes week/day controls. |
| C2 | PASS | Wrap-up: one-line status expands date range, `Asia/Shanghai`, current-day inclusion and cost. |
| C3 | PASS | Wrap-up: weekly hover tooltip title `08/25–08/31` and full agent breakdown. |
| C4 | PASS | `24`: selected-week heading and two model disclosures; current-week panel has no delta. |
| C5 | PASS | `09` and wrap-up: daily mode has 30 bars and chart-only horizontal scrolling; week→day clears the pin. |
| C6 | PASS | `10`: seven bars and today pin restored; aggregation controls disappear. |
| C7 | PASS | `25` and wrap-up: Codex filter produces only Codex-colored segments in both week and day modes. |
| D1 | PASS | `20`: unpriced synthetic model gives `成本 n/a ⓘ`, missing-price tooltip, no fake `$0.00`. |
| D2 | PASS | `26` shows the full no-usage reason for a historical empty day; a separate no-usage-today launch shows the same short mark and `无用量` tooltip in `31`. |
| D3 | PASS | A priced historical day shows `预估成本 $0.19` with no n/a mark (wrap-up output). |
| D4 | **FAIL (P2)** | `27` / `src/features/usage/i18n.ts`: the explainer retains the cache denominator and missing-price rule, but omits the no-usage cost state. See defect below. |
| E1 | PASS | `28`: 420px viewport, document scroll width 410px; header and controls remain readable. |
| E2 | **NOT RUN** | 200% Windows text scaling requires a controlled display session; no OS setting was changed or emulated. |
| E3 | PASS | Core reduced-motion value `1e-05s` = `0.01ms`; wrap-up observed refresh animation iteration count `1` and no transition residue. |
| E4 | **PASS (theme proxy)** | `12`, `32`: `emulateMedia({ colorScheme: "light" })` keeps sigils, underlines, composition and weekly bars discernible. A real OS light-theme change plus application restart remains NOT RUN. |
| E5 | PASS | A pre-click DOM observer saw the disabled `刷新中…` button and timestamp dot during manual collection, then the dot disappeared; `29` is a settled-state screenshot, not a claim that it captured the transient dot. |
| E6 | **NOT RUN** | `window.dispatchEvent(new Event("focus"))` did not trigger the dot. Source listens to Tauri `getCurrentWindow().onFocusChanged`, so that DOM event is not a valid proxy for native refocus; a controlled native switch remains required. |
| E7 | PASS | `30` and wrap-up: English agent/type, expand/collapse, cost n/a, week/day, close-detail and prior-week text render without Chinese residue; switched back to Chinese. |

Screenshot inspection found no additional visible overflow, clipping or theme
contrast regression within the tested 125% DPI/viewport conditions. The 420px
and light-face checks used WebView2 viewport/media emulation; they do not prove
native resize or OS-theme restart behavior.

**Defect D4 (P2, documentation copy):** With the isolated unpriced/empty-day
profile, open `关于这些数字`. Expected: the cost explanation distinguishes priced,
missing-price and no-usage states while retaining the cache-read denominator.
Actual: the denominator and missing-price / no-fake-zero rules are present,
but the no-usage state is absent. The UI's no-usage tooltip and historical
detail remain correct (`26`, `31`). Suggested priority P2; no product code was
changed in this walkthrough.

**D4 disposition (later on 2026-09-23): FIXED / RETESTED.** Commit `d82f6d3`
adds the no-usage cost state and reserves `$0.00` for usage with a complete
zero-cost estimate in both languages. The added English Edge E2E assertion
passed; the final native EXE's Chinese explainer exposed `没有用量` and preserved
`缓存读取` with isolated synthetic data (`native-dark100.log`,
`native-light100.log`). The `27` screenshot and initial FAIL above remain the
historical first attempt, not a rewritten PASS.

## Clean validation environment

Detached worktree `D:/DevTools/cam-v050-clean-20260923` was checked out at
`2927799940b9d37e4e84707e0b4837368fe00add`; source, `node_modules`,
`dist` and Cargo `target` were not copied from the development checkout.
`pnpm install --frozen-lockfile` passed with the existing pnpm store. Installed
compiler/SDK and Cargo registry were reused. Toolchain: Node 20.20.2, pnpm
10.33.0, rustc/cargo 1.98.0, Windows 11 10.0.26200 x64. This is clean-source
validation on the current host, not a developer-tools-free clean OS. The
worktree is clean; raw local logs, screenshots, synthetic records and the
temporary backup are under ignored `.scaffold-root/v050-evidence/`.

The first final-source `pnpm tauri build` passed but emitted a release-only
unused `Mutex` import warning. Commit `2927799` gates that test-only import;
the final clean-worktree rebuild passed with only the localized MSVC linker
library-creation message. The first `cargo fmt --check` attempt failed on two
formatting-only test lines before `d3f99f2`; the fix and subsequent passes
are separate evidence. An extra `cargo clippy --release --all-targets -- -D
warnings` attempt failed because existing integration tests invoke the
debug/test-only `set_worker_exe_override` in release mode. This extra command
is not the specified debug all-targets gate; final strict debug all-targets
Clippy and release `--lib --bins` Clippy both passed. No failed attempt is
reported as passing.

## Native acceptance and release decision

**LOCAL BUILD, INSTALLER LIFECYCLE AND CONTROLLED-DISPLAY ACCEPTANCE COMPLETE /
READY FOR LOCAL CANDIDATE HANDOFF; NOT READY FOR PUBLICATION.** All v0.5 current-host build, package inspection, controlled
display and fresh/upgrade lifecycle checks below were rerun against the final
candidate. Every app launch used an isolated synthetic profile; no actual
usage was displayed. The v0.4 verdict was not transferred.

| Check | Result and evidence |
| --- | --- |
| Native 125% and 150% DPI | **PASS.** The primary monitor reports 120 DPI and final EXE `devicePixelRatio=1.25`. The second monitor already runs at 144 DPI; moving the final native window there gave `devicePixelRatio=1.5`, 680px CSS viewport / 670px document width and no clipping in `native-dark150.png` (CDP screenshot for layout). No system scaling value was changed. |
| Native minimum width | **PASS.** Resizing the actual Windows window to its 420px content minimum produced a 420px viewport / 410px document width, with readable header and detail in `native-min420.png`. The first 525px outer-width attempt did not reach the minimum and remains in `native-min420.log`; `native-min420-attempt2.log` is the passing run. |
| 200% Windows text size | **PASS, scoped to the current host.** Windows Settings > Accessibility > Text size slider was applied from 100 to 200; `TextScaleFactor=200` and final EXE WebView2 DPR 2.5 confirmed the OS change on the 125% monitor. The 340px CSS viewport had 330px document width and no horizontal overflow; `native-dark200.png` shows readable, vertically scrollable content. The setting was restored to 100 and its originally absent registry override removed. A later repeat after manually removing that value moved the slider but did not apply to the OS (registry absent, DPR 1.25); `native-text200-final.log` retains this invalid attempt and it is not counted as another pass. |
| Native reduced motion | **PASS.** Windows Settings > Accessibility > Visual effects > Animation effects was switched off and `MinAnimate=0` observed before launching the final EXE. A direct WebView2 CDP connection without Playwright media emulation reported `prefers-reduced-motion: reduce`, transition duration `1e-05s` (0.01 ms), and refresh-icon animation duration `1e-05s` / iteration count `1` (`native-reduced-direct.log`). The setting was restored to On / `MinAnimate=1`. The first attempt sampled before OS propagation; the second used Playwright, which applied its own default media overrides. Both failed probes remain retained and are not counted as native passes. |
| Native dark/light restart | **PASS.** Before Playwright CDP attachment, the dark-host final EXE rendered the dark surface (`native-dark100-pre-cdp.png`); direct CDP without media emulation also reported `light=false` (`native-media-direct-baseline.log`). Setting both Windows app/system light-theme values to 1, broadcasting the theme change and starting a new final EXE yielded Tauri native theme `light`; the pre-CDP system screenshot `native-light100-pre-cdp.png` shows the light surface and readable sigils, controls and bars. Both values were restored to their original 0. Playwright attachment reported light media even on the dark host, so its screenshots are not used as proof of native theme selection. |
| Native focus refresh | **PASS on final EXE.** A real foreground switch to Windows Settings and back produced `tauri://blur` then `tauri://focus`, and the DOM observer saw the working dot (`native-focus-attempt.log`). Earlier attempts where Windows foreground locking rejected a switch or no dot was seen remain in `native-dark200-attempt2`–`attempt5` logs; DOM `focus` dispatch is still only the invalid first-round proxy. |
| MSI administrative extraction and identity | **PASS.** `msiexec /a` exited 0. The extracted application directory contains only `coding-agent-monitor.exe` (15,081,984 bytes, version 0.5.0), `LICENSE` (1,111 bytes) and `THIRD_PARTY_NOTICES.md` (6,031 bytes), with no external sidecar. MSI ProductCode `{CCCFC155-0D32-4922-A4DE-2F0DA12F9B47}` differs from v0.4, UpgradeCode `{4A24D63D-3149-5CC0-97CA-01A4CE86F462}` matches it. |
| NSIS generated input | **PASS (static build-input audit).** The final `target/release/nsis/x64/installer.nsi` declares 0.5.0, x64, current-user mode and exactly the product EXE plus the two notice files as application payload. It includes `installer-hooks.nsh` for old sidecar cleanup and PerMonitorV2 awareness for the installer. This is an exact generated-input audit, not independent binary unpacking. |
| Packaged worker | **PASS.** The MSI-extracted EXE and final NSIS-patched source image each handled explicit synthetic `claude` and `claude-desktop` requests: exit 0, status `ok`, one 160-token record and empty stderr (four cases; `worker-smoke-attempt3.log`). The first two smoke attempts used a fixture layout/JSON spacing that the vendor's fast scanner skipped; those raw failures are retained and are harness defects, not product failures. |
| NSIS fresh and v0.4→v0.5 upgrade | **PASS.** Silent fresh install registered 0.5.0 and only EXE, two notices and uninstaller; installed EXE launched with synthetic data and a second launch exited 0 while one main process remained. Uninstall exited 0 and removed directory, HKCU registration and CAM processes. A separate v0.4 install then v0.5 in-place upgrade changed registered/EXE version 0.4.0→0.5.0; upgraded app and single-instance check passed; final uninstall left the same zero residue. |
| MSI fresh and v0.4→v0.5 upgrade | **PASS with path note.** Standard per-user install properties yielded exit 0. Windows Installer reused the host's remembered `D:/DevTools/test/Coding Agent Monitor` location despite the requested isolated `INSTALLDIR`. Before both lifecycles, its four pre-existing files were backed up and SHA-256 verified. Fresh install and v0.4→v0.5 upgrade each registered only ProductCode `{CCCFC155-0D32-4922-A4DE-2F0DA12F9B47}` at 0.5.0; both installed EXEs launched with synthetic data and passed the second-launch check. Both final uninstalls exited 0, left zero CAM MSI registrations/processes, and the four original files were restored byte-for-byte from the backup (including the original EXE hash `c758f1dfde96cdd79c88d30e735ffb7efd43efd8d83aeb439749367549445e66`). This is not a clean-path claim. |
| Preference disk-failure and retry | **PASS on the final NSIS-installed EXE.** With an isolated synthetic profile, English saved to `preferences.json`; an empty directory placed at `preferences.json.tmp` forced the next save to fail. The UI reverted to English, displayed “This preference could not be saved.”, and the preferences file hash stayed unchanged. Removing the obstruction and retrying Chinese persisted successfully (`nsis-installed-prefs.log`). The same sequence passed on the final unbundled EXE (`native-prefs.log`). The installed EXE also passed a second-launch single-instance check. |
| Native tray menu and Exit | **PASS on the final release EXE before installation.** Windows UI Automation exposed the localized menu summary `今日: 8.04K · 最近成功 17:02 · Claude Code 8.04K` and `立即刷新`, `打开主界面`, `退出` items; `native-tray-menu-crop-correct.png` is a crop of the native desktop capture. Invoking Refresh kept the process alive, Open made the dashboard visible, and Exit left zero CAM processes. This used synthetic usage and the final clean-build EXE, not an installed payload. The first launch attempt failed to open WebView2 while an existing CAM instance ran; its raw tool output remains in the task transcript, but the redirected log was overwritten by the subsequent passing run. |
| Installed-package tray | **PASS on the final NSIS-installed EXE.** A fresh install from the local delivery copy exited 0 and contained only the EXE, two notices and uninstaller. With an isolated synthetic profile, Windows UI Automation found the localized usage summary and all three menu actions. Invoking `立即刷新` produced the WebView2 working dot; `打开主界面` made the dashboard visible; `退出` ended the process with code 0 (`nsis-installed-tray-final-attempt3.log`). Silent uninstall exited 0 and left no installation directory, CAM process or HKCU uninstall registration. The first new attempt failed because Windows PowerShell misparsed UTF-8 Chinese in the test script; the second did not open the menu with its first click method. Both harness failures remain in `nsis-installed-tray-final.log` and `nsis-installed-tray-final-attempt2.log`. The earlier full-screen-blocked attempt remains in `nsis-installed-tray.log`; none of these attempts is rewritten as a pass. |
| External lifecycle | **NOT RUN.** Developer-tools-free/offline clean host, real logon/reboot, sleep/resume and full non-ASCII Windows profile remain to be exercised or explicitly disposed. |

Still required before public publication / carried-over NOT RUN:

1. Developer-tools-free/offline clean-host run (Windows Sandbox unavailable
   on this host; no isolated VM provided).
2. Real logon/reboot startup, sleep/resume, full non-ASCII Windows profile.
3. Publication itself (tag, GitHub Release, upload) — no tag or push has been
   performed for any version from this working environment.

## Local delivery directory

**PASS.** Local candidate delivery is at
`D:/DevTools/CodingAgentMonitor-v0.5.0-delivery` with exactly six files: MSI,
NSIS installer, `SHA256SUMS.txt`, `Release Notes.md`, `SOURCE_IDENTITY.txt`,
`Acceptance Record.md`. Installer hashes were rechecked from inside that
directory. This is a local handoff, not publication approval.

## Final-build installers

Build directory: detached clean worktree `D:/DevTools/cam-v050-clean-20260923`.
Full build-source SHA: `2927799940b9d37e4e84707e0b4837368fe00add`.
Artifacts are under `src-tauri/target/release/bundle/{msi,nsis}` there.
Both Authenticode checks returned `NotSigned`.

| File | Bytes | SHA-256 |
| --- | ---: | --- |
| `Coding Agent Monitor_0.5.0_x64_en-US.msi` | 5,513,216 | `f2f9751069053cefd679046e2d99576217f2c2de327c4b348e8e72b4e394c333` |
| `Coding Agent Monitor_0.5.0_x64-setup.exe` | 3,610,481 | `874f7616531bdbfff1952f0f6f71cab16ba7d2e4f1d58ae5bf45224cdb99d88a` |
