# v0.5.0 release verification

> **SKELETON — opened 2026-09-18.** Every result below is `NOT RUN` until
> evidence lands here. Follow the v0.4.0 discipline: record the exact
> build-source SHA with the package, keep failures as separate retained
> attempts, never relabel a historical waiver as a pass, and do not publish
> while gates remain open.

Date: TBD. Final build-source commit: TBD (record the full SHA of the commit
the packages are built from; expected to include the 0.5.0 version bump plus
the Claude Desktop collector work once committed).

## Source and fixes (expected contents)

- `6d00817`: control-language / trend-readability style pass that predates
  the main-screen refactor.
- `dfe8690`: P0 main-screen refactor — trend chrome consolidation, today
  composition strip, today pinned by default (review A1/B1/C1).
- `45ba1a2`: P1 marks & controls language — sigils, underline tabs, cost
  demotion, 30-day week aggregation (marks §2–3, review A2/B2).
- `c46aac2`: P2 chart label hierarchy and refresh-presence dot (review
  A4/C3).
- TBD: Claude Desktop collector backend (new `claude_desktop.rs`, collector
  registration, tests) — currently uncommitted; must land with its Cargo
  test/clippy evidence before packaging.
- TBD: the version-bump commit containing this file set.

## Prerequisites before packaging (fill in Windows)

| Check | Current result |
| --- | --- |
| cargo fmt -- --check / strict Clippy / Cargo check (incl. Claude Desktop backend) | NOT RUN |
| Cargo test (incl. new Claude Desktop suites) | NOT RUN |
| pnpm lint / typecheck | NOT RUN on Windows this cycle |
| pnpm test | NOT RUN on Windows this cycle (152 unit tests green in the 2026-09-18 sandbox run) |
| pnpm test:e2e | **PARTIAL (2026-09-18 sandbox): 47 passed** under headless Chromium on Linux against `pnpm dev` + the e2e harness. This is not the release gate: re-run `pnpm test:e2e` on Windows (system Edge channel) and record here. The first full run exposed a real 680x700 overflow fixed in `5e2dfe8`. |
| pnpm vendor:verify | NOT RUN |
| pnpm build | NOT RUN on Windows this cycle |
| Manual UI walkthrough: 680x700 default window, 125% DPI, 200% text scaling, reduced-motion residue, composition strip, pinned-today panel, filter tabs keyboard path, week↔day switch | NOT RUN — checklist in the 2026-09-18 delivery report |

## Clean validation environment

TBD — mirror v0.4.0: detached worktree from the build-source commit, fresh
`pnpm install --frozen-lockfile`, reused store/registry only. Record the
toolchain versions and the ignored evidence directory (suggest
`.scaffold-root/v050-evidence/`).

## Native acceptance and release decision

TBD. Expected structure, carried over from v0.4.0: fresh NSIS and MSI
install/upgrade/uninstall lifecycles on the current Windows 11 x64 host with
isolated data; packaged-worker smoke on synthetic data; tray and preference
exercises. The v0.4.0 verdict ("local native acceptance complete / ready for
local delivery; not ready for publication") does not transfer — v0.5.0
re-opens every gate with its own evidence.

Still required before public publication / carried-over NOT RUN:

1. Alternate 125%/150% DPI and 200% text scaling in a controlled display
   session (v0.4.0 never ran these; the 2026-09-18 UI work has additionally
   not been visually inspected at any scaling).
2. Developer-tools-free/offline clean-host run (Windows Sandbox unavailable
   on this host; no isolated VM provided).
3. Real logon/reboot startup, sleep/resume, full non-ASCII Windows profile.
4. Publication itself (tag, GitHub Release, upload) — no tag or push has been
   performed for any version from this working environment.

## Local delivery directory

TBD — v0.4.0 convention: six files (MSI, NSIS installer, `SHA256SUMS.txt`,
`Release Notes.md`, `SOURCE_IDENTITY.txt`, `Acceptance Record.md`) in a
dedicated delivery folder, hashes re-verified from inside the directory.

## Final-build installers

TBD — record build directory, full build-source SHA, artifact paths, and the
SHA-256 / byte size of the final MSI and NSIS files.
