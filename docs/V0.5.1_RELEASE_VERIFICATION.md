# v0.5.1 release verification

> **OPEN — local candidate, not released.** This is a result ledger, not a
> pre-approved release decision. The automated results dated 2026-09-25 are
> **pre-freeze candidate checks on a modified worktree**, not validation of a
> final build-source commit or installers. Unchecked rows remain `NOT RUN`.
> Retain failed attempts and distinguish waivers from technical passes.
> v0.5.0 evidence is background only.

## Candidate identity

| Field | Value |
| --- | --- |
| Published baseline | `main` `6f86eb1` (v0.5.0 publication documentation) |
| Candidate build-source commit | **PENDING** — record full SHA after source is frozen |
| Candidate manifest version | `0.5.1` in package, Cargo and Tauri manifests; recheck at build |
| Pre-freeze automated checks | 2026-09-25, modified v0.5.1 worktree; final source identity **PENDING** |
| Final Windows build environment / date | **NOT RUN** |
| Worktree cleanliness at build | **NOT RUN** |
| Release decision | **PENDING** |

## Automated checks

The following commands completed on 2026-09-25 in the modified v0.5.1
worktree. Results are useful candidate evidence, but must be tied to a frozen
source SHA and repeated as needed for the final clean build. The task's command
output is the current evidence; final log locations and source identity remain
to be entered. A passing rerun does not erase an earlier failure.

| Check | Result | Evidence / notes |
| --- | --- | --- |
| `pnpm install --frozen-lockfile` | **PASS — candidate** | Completed on the modified worktree. |
| `pnpm lint` | **PASS — candidate** | Exit 0. |
| `pnpm typecheck` | **PASS — candidate** | Exit 0. |
| `pnpm test` | **PASS — candidate** | 15 files, 147 tests passed. |
| `pnpm test:e2e` | **PASS — candidate** | 47/47 passed. |
| `pnpm build` | **PASS — candidate** | Frontend build completed; this is not an installer build. |
| `pnpm vendor:verify` | **PASS on rerun — candidate** | The first attempt before staging the refreshed vendor files failed six inventory checks against the old index. After staging the vendor files, the rerun passed all checks. The first failure is retained here as a staging-state mismatch, not a product test pass. |
| `node scripts/vendor-ccusage-import.mjs --check-staged` | **PASS — candidate** | 207 staged vendor files matched the pinned source byte-for-byte. |
| `cargo fmt --check` | **PASS — candidate** | Product formatting gate completed. |
| `cargo check --manifest-path src-tauri/Cargo.toml --all-targets` | **PASS — candidate** | Exit 0. |
| `cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings` | **PASS — candidate** | Exit 0. |
| `cargo test --manifest-path src-tauri/Cargo.toml` | **PASS — candidate** | Product suite exited 0; test count not recorded here. |
| `cargo test --manifest-path src-tauri/vendor/ccusage/rust/Cargo.toml --workspace` | **PASS — candidate** | Vendor workspace suite exited 0; test count not recorded here. |
| `pnpm tauri build` | NOT RUN | — |

## Focused behavior and price checks

The suite results above do not establish each scenario below. These rows stay
`NOT RUN` until case-specific evidence and its source identity are recorded.

| Scenario | Result | Evidence / notes |
| --- | --- | --- |
| Success with recoverable skipped records: no coverage notice in main, 30-day metadata or selected-day detail, in Chinese and English | NOT RUN | Use isolated synthetic records; confirm 7/30-day and week/day views. |
| Public Rust/TypeScript summary retains coverage and diagnostics; no path, raw record or secret leaks | NOT RUN | Record contract/serialization test evidence. |
| Structured Agent failure, failed refresh and stale snapshot remain visible as failures or stale data | NOT RUN | Record test/native evidence. |
| LiteLLM/models.dev pins, SHA-256, entry counts, retained old-only IDs and reproducibility | NOT RUN | Compare manifest, vendor verification and staged-source check. |
| Representative public rates; missing price, mixed price and true zero cost semantics | NOT RUN | Include GPT-5.6 Sol, GPT-6 Sol, Claude Opus 5.5, Gemini 3.8 Flash and an unpriced Antigravity variant where applicable. |
| No runtime pricing fetch; older logs can be recalculated with the new snapshot | NOT RUN | Record implementation/test evidence; do not claim bill parity. |

## Native and installer acceptance

| Scenario | Result | Evidence / notes |
| --- | --- | --- |
| Native Windows 11 x64 UI, both languages, 125%/150% DPI, 200% text size, reduced motion and theme restart | NOT RUN | Record actual conditions separately; browser emulation is only a proxy. |
| Fresh install, v0.5.0→v0.5.1 upgrade, uninstall, single instance, same-EXE worker and tray actions | NOT RUN | Record MSI/NSIS separately and use isolated synthetic usage. |
| Developer-tools-free offline clean host | NOT RUN | No v0.5.0 result carries forward. |
| Real logon/reboot and sleep/resume | NOT RUN | No v0.5.0 result carries forward. |
| Full GUI cycle under a real non-ASCII Windows user profile | NOT RUN | No earlier waiver or gap counts as a pass. |
| Authenticode state / SmartScreen disclosure | NOT RUN | Check final installer bytes. |

## Final packages and publication

| Asset | Bytes | SHA-256 | Signing state |
| --- | ---: | --- | --- |
| MSI | PENDING | PENDING | PENDING |
| NSIS | PENDING | PENDING | PENDING |

Source SHA, installer identity, public checksum file and downloaded asset
hashes: **PENDING**. Git tag, PR/CI, Release URL and maintainer disposition:
**PENDING**. Do not mark this version released or transfer any v0.5.0 waiver
until those actions and their results are recorded.
