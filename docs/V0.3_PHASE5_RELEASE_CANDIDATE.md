# v0.3 Phase 5 — Sidecar 供应链删除与 Release Candidate 验收记录

日期：2026-09-01 · 基线：Phase 4B（`3a00497`）
Phase 5 提交：`941ccf8`（供应链删除，独立可回滚）+ 本验收提交。

## 1. 删除清单（commit `941ccf8`）

| 类别 | 删除内容 |
| --- | --- |
| 二进制/staging | `src-tauri/binaries/ccusage-x86_64-pc-windows-msvc.exe`、`ccusage-antigravity-x86_64-pc-windows-msvc.exe`、`binaries/README.md`（staging 约定） |
| 下载/构建脚本 | `scripts/fetch-ccusage.mjs`、`scripts/fetch-ccusage-antigravity.mjs`（npm 下载 URL、SHA-512/256 校验、PR #1487 本地构建逻辑） |
| package.json | `fetch:sidecar` 命令（无 postinstall/prebuild hook 存在） |
| CI | `.github/workflows/ci.yml` 的 "Stage pinned sidecars" 步骤 |
| 打包 | `tauri.conf.json` 的 `externalBin` 两项；`build.rs` 的 sidecar 存在性门禁 |
| 运行时 | `src/sidecar/runner.rs` 全部（packed/dev 路径解析、`Command::new` 启动、15s 超时监督、COLLECT_STATE 缓存、ACTIVE_CHILDREN/taskkill 树杀）；`AppError::sidecar_missing/failed/timeout` |
| 依赖 | sidecar 路径独占的 npm/Rust 依赖：无（fetch 脚本仅用 Node 内置；runner 仅用 std） |
| 权限 | Tauri capabilities 仅含 `core:default`（无 shell/plugin sidecar 权限） |

保留（分类依据见任务清单 §一）：

- **历史设计文档**：`docs/V0.2_*`、`docs/V0.3_DEVELOPMENT_AND_ACCEPTANCE_PLAN.md` 等——记述 v0.2 sidecar 架构，作为迁移历史保留；
- **opt-in 开发审计工具**：`tests/shadow17.rs`、`tests/sidecar_shadow.rs`——sidecar 路径改为显式环境变量 `CAM_SHADOW_SIDECAR_EXE` / `CAM_SHADOW_ANTIGRAVITY_EXE`，默认构建/测试不要求、不查找、不下载 sidecar，永不打包，不被生产代码引用；
- **`adapter::normalize_reports`**（v0.2 sidecar JSON 解码器）：仅为上述 opt-in 审计工具保留；生产唯一入口是 `normalize_snapshot`（静态审计测试钉死）；
- **vendored ccusage 源码**（`src-tauri/vendor/ccusage/`）：产品采集引擎本身，含 LICENSE、UPSTREAM.toml、PATCHES.md、pricing manifest。

## 2. 测试数量变化与替代覆盖

| 二进制 | Phase 4B | Phase 5 | 变化 |
| --- | --- | --- | --- |
| lib unit | 42 passed + 1 ignored | 39 passed | **−3 active, −1 ignored**：删除的 `runner.rs` 内含 4 个单元测试——3 个（timeout reaping、stdout capture、non-zero exit）测试 sidecar 进程监督机制，等价路径由 `worker_integration.rs`（22 passed，未变）的 worker timeout/cancel/exit 测试覆盖；1 个 ignored 的"真实 sidecar 七日摘要"测试依赖本机安装 sidecar，随供应链删除移除 |
| 其余 13 个二进制 | — | 149 total passed | 全部不变（golden、契约、协议、语义、production_switch 14、production_shutdown 1、shadow17 3、sqlite_safety 6 等） |
| **合计** | **152 passed + 1 ignored** | **149 passed + 0 ignored** | 核心回归覆盖完整；无覆盖率缺口 |

opt-in shadow harness 可用性：不设环境变量时逐测试 SKIP 并打印说明；设置后即恢复
17-Agent 逐字段对照能力（Phase 4A/4B 已验证该能力本身）。

## 3. 生产链静态/运行时不可达验证

- `production_chain_source_has_no_sidecar_runner_references`：生产链 7 个源文件无
  sidecar runner 符号（PASS，套件内）；
- `production_chain_never_executes_a_sidecar_poison_marker` + 进程观测测试（PASS）；
- release EXE 字符串审计：`ccusage.exe`、`ccusage-antigravity`、`fetch-ccusage`、
  `registry.npmjs.org/@ccusage`、`externalBin`、开发路径（`DevTools`）、测试 seam
  （`CAM_TEST_WORKER`/`CAM_SHADOW*`）全部 **0 命中**；
- worker 仅经 `current_exe()` 启动自身（supervisor `worker_exe()`，无 PATH 搜索）。

## 4. 联网 vendor 重建 Gate —— **PASS**

`node scripts/vendor-ccusage-import.mjs`（2026-09-01，纯网络流程）：

- shallow-fetch pinned `ccusage v20.0.20`（commit `bd7f89b…`，tree `0acb7f0e…`）、
  PR #1487 fork（commit `c58c1b3…`）、LiteLLM 定价（commit `1a183ef…`，SHA-256 验证）；
- 应用可重放 patch series（`patches/0002-cam-downstream-v20.0.20.patch`）；
- **207 个文件与提交的 vendor 树逐字节一致**（git blob 级 diff = 0）；
- 期间修复了导入脚本两个工具缺陷（工作树 CRLF 检出污染补丁字节比较；跨盘
  EXDEV rename 使 swap 半途失败）——修复属重建工具本身，不改变 vendored 内容；
- GitHub 访问存在间歇性 SSL 失败，重试后完成（记录于脚本日志）；
- 未更新到任何更新 upstream 版本；基线与 `UPSTREAM.toml` 逐字节一致；
- 重建后 `pnpm vendor:verify` PASS、vendor workspace 测试 490 passed。

## 5. 安装包构建与内容审计（v0.3.0）

| 产物 | 大小 | SHA-256 |
| --- | --- | --- |
| coding-agent-monitor.exe（release） | 14,774,784 B | `115992f70a6f9e2bf041c9e3489417bd9088b421e00e3f018086a293ffdbde3a` |
| MSI `Coding Agent Monitor_0.3.0_x64_en-US.msi` | 6,049,792 B | `8d611f6a33e3df611da672df375b937e155b9a0e4b52505d615ade72659bcbf6` |
| NSIS `Coding Agent Monitor_0.3.0_x64-setup.exe` | 3,525,596 B | `633688819bff369f413847c7aabfd4ba660b3a34292d14da2c16779baa85161b` |

（最终产物于 2026-09-01 在冻结源码 `6435d54` 上以 `pnpm tauri build` 重建并重新审计；
本表为此前构建中途记录的 hash 的更正——升级 hook 重建与最终重建都晚于最初记录。）

**MSI 文件表（msilib 枚举，4 项）**：

| 文件 | 大小 |
| --- | --- |
| coding-agent-monitor.exe（唯一 .exe） | 14,774,784 |
| coding_agent_monitor_lib.dll（产品组件） | 1,194,496 |
| LICENSE | 1,090 |
| THIRD_PARTY_NOTICES.md | 6,031 |

无 ccusage 命名文件、无下载器/脚本/调试工具、无 shadow fixture/测试数据库。
NSIS 与 EXE 字符串审计同样零命中。产品 EXE hash 已记录于上表。

## 6. 安装/运行/升级/卸载验收（Windows 11 家庭版 10.0.26200，x64）

| 项 | 结果 |
| --- | --- |
| MSI 静默安装 | perMachine 需管理员（Error 1925，非交互 shell 未提权）——MSI 走安装器正常 UAC 流程；本验收用 NSIS per-user 完成全生命周期 |
| NSIS 静默安装 | PASS |
| 升级覆盖 v0.2（真实场景：v0.2 NSIS 安装目录） | PASS——产品 EXE/通知文件更新；**新增 NSIS hook 在升级时清除 v0.2 遗留的 `ccusage*.exe`**，升级后目录仅含产品 EXE + LICENSE + THIRD_PARTY_NOTICES.md + uninstall.exe |
| 启动 | PASS：单 UI/托盘主进程 |
| 刷新（启动/周期） | PASS：仅一个瞬时同名 worker（真实数据冷刷 ~3s，后台执行）；零 ccusage 进程 |
| worker 无控制台 | PASS（CREATE_NO_WINDOW） |
| 退出/再启动 | PASS：关闭窗口 = 收入托盘（v0.2 一致行为）；进程无残留 |
| 已有应用 DB 保留 | PASS：升级/卸载均保留 `%APPDATA%\com.codingagentmonitor\usage-cache.sqlite3` |
| Agent 源数据未修改 | PASS：卸载前后 `~/.claude` 数据字节数一致（± 正常 Agent 自身写入） |
| 中文/空格用户路径 | PASS：安装于 `D:\DevTools\test\Coding Agent Monitor`（含空格路径）运行正常 |
| 卸载 | PASS：静默卸载移除安装目录与注册表项，不触碰用户 Agent 数据 |
| supervisor 不终止其他 CAM 实例 | PASS（自动化回归测试） |

## 7. 签名与安全

- **Authenticode**：EXE/MSI/NSIS 均为 `NotSigned`（未提供证书，不自行签名）。
  **签名 Gate 保持 BLOCKED**；Release Notes 披露 SmartScreen 风险与 SHA-256 核对方式。
- **Defender 扫描**：三个产物 `found no threats`。
- **运行时网络**：刷新期间产品进程 0 条远程 TCP 连接（仅本机文件/管道采集）。

## 8. 最终测试结果

| 项 | 结果 |
| --- | --- |
| `git diff --check` / `cargo fmt --check`（双工具链） | PASS |
| `cargo check --all-targets --offline`（双工具链） | PASS，0 警告 |
| `cargo test --offline`（stable 1.98.0） | **149 passed, 0 failed**（逐二进制见 §2） |
| `cargo test --offline`（pinned 1.97.1，独立 target） | PASS：**149 passed, 0 failed**（与 stable 完全一致） |
| vendor workspace check/test | PASS：490 passed |
| 前端 unit tests / Playwright E2E | PASS：72 / 13 |
| `pnpm vendor:verify` / license inventory | PASS / 288 allowed, 0 blocked |
| SQLite 单一实现 | PASS：仅 `sqlite v0.37.0`；`rusqlite`/`libsqlite3-sys` = 0 |
| 17-Agent golden + 契约/协议/语义/进程 | PASS（套件内） |
| 20 并发公开 command single-flight | PASS |
| release 生产路径性能 | PASS：median ≈ 132 ms / p95 ≈ 143 ms（桌面运行应用+Defender 扫描同机环境；Phase 4B 静默环境为 75/86 ms；p95 Gate ≤1s 余量 7×） |
| release 字符串审计 / MSI 文件清单审计 | PASS（§3/§5） |
| 安装/升级/卸载生命周期 | PASS（§6） |
| 生产调用图不可达 sidecar | PASS（§3） |

## 9. 本记录时的开放 Gate —— 已在最终决策中闭环

本记录撰写时的状态是 release candidate（BLOCKED）。以下 Gate 的最终处置见
[`docs/V0.3_RELEASE_GATE_DECISION.md`](V0.3_RELEASE_GATE_DECISION.md)（维护者于
2026-09-01 逐项书面批准 waiver，状态改为 **READY TO RELEASE**）：

1. Authenticode 签名 → **Waiver #1**（unsigned release，延续 v0.2 策略 + SHA-256 披露）；
2. Windows 10 x64 安装生命周期 → **Waiver #2**（支持声明改为仅 Windows 11 x64）；
3. 非 ASCII 用户目录 GUI Gate → **Waiver #3**（v0.3 重新批准，不自动沿用 v0.2 waiver）；
4. 正式发布动作（push / tag / GitHub Release）→ 仍属维护者的明确独立授权，本仓库
   内的任何自动化均未执行。
