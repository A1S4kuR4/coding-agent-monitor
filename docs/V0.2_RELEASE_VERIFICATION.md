# Release Verification Record — Coding Agent Monitor v0.2

**Date:** 2026-08-28
**Environment:** Windows 11 Home China, x64, ASCII-named user profile (username redacted)
**Code under verification:** branch `feat/v0.2-token-residuals`, commit `6bc54f5` (code) + `91a2f3a` (record), on top of the published v0.1.0 code base.
**Baseline sidecars:** ccusage 20.0.20 (official) + pinned Antigravity focused compatibility build (upstream PR #1487, commit `c58c1b3…`).

All outcomes below are actual, run live. Any item not completed is explicitly marked **NOT RUN** or **BLOCKED** rather than assumed. This record does not modify v0.1 history (`RELEASE_VERIFICATION.md`).

---

## 1. Automated check suite — PASS

Per the v0.2 plan §5.4 full set, run on 2026-08-28:

| Check | Result |
|---|---|
| `pnpm lint` | **PASS** |
| `pnpm typecheck` | **PASS** |
| `pnpm test` (vitest) | **PASS** — 11 files, 72 tests |
| `pnpm build` | **PASS** — vite production build |
| `pnpm test:e2e` (Playwright) | **PASS** — 13 tests (see the worker-count note below) |
| `cargo fmt -- --check` | **PASS** |
| `cargo clippy --all-targets -- -D warnings` | **PASS** |
| `cargo check` | **PASS** |
| `cargo test` | **PASS** — 37 passed, 1 ignored (the ignored one is the real-data smoke, run explicitly in §2) |
| `pnpm tauri build` | see §4 |

> **E2E worker note:** with the default 13 parallel Playwright workers, 5 tests failed with Chromium renderer `Fatal process out of memory: Zone` crashes — an environment concurrency limit, not a code regression. Re-run with `pnpm test:e2e -- --workers=2`: **13/13 PASS**. Future runs on this machine should use a reduced worker count.

## 2. Real-data smoke — PASS

Command: `cargo test --manifest-path src-tauri/Cargo.toml -- --ignored --nocapture`

- Result: **PASS** — 1 passed, 37 filtered out; test duration ~1.3–1.8 s.
- Observed shape (values from the verifier's own machine, structural only): a valid 7-day summary was produced from local logs with no Node/pnpm/network; three dynamic agents appeared (`codex`, `claude`, `antigravity`) with descending token totals; day-level estimated cost was correctly reported as unavailable (`cost=None`, not faked to $0.00); cache-input share resolved to a finite ratio (~0.95); `collectedAt` carried a current UTC timestamp; the 7-day series contained no gaps.

## 3. Data-path and contract scenarios — PASS (unit level)

Covered by the current unit suites (no change from the v0.1 binary-level findings):

| Scenario | Coverage | Result |
|---|---|---|
| Unified report parsing, missing/duplicate/out-of-order days, 0/1/2/6 agents, unknown agent fallback | adapter unit tests (`adapter.rs`) | **PASS** |
| Malformed JSON, invalid period, type errors, unknown extra fields | adapter unit tests | **PASS** |
| Token totals vs component aggregates, saturating adds, declared-total disagreement | `emit_day_total` checks (§12 #6) | **PASS** |
| Cost zero / missing / non-finite never enters the contract | adapter cost gating | **PASS** |
| Cache-share zero denominator → unavailable (no NaN/Infinity) | `cacheInputShare` + adapter tests | **PASS** |
| u64 → JS `number` safe-integer bound | `MAX_SAFE_INTEGER` check (§12 #3) | **PASS** |
| Timeout / abnormal exit / child-tree reaping | runner tests | **PASS** |

## 4. Installer build — PASS

Fresh `pnpm tauri build` (2026-08-28, exit 0) produced:

| Artifact | SHA-256 |
|---|---|
| `src-tauri/target/release/bundle/msi/Coding Agent Monitor_0.1.0_x64_en-US.msi` | `0dd3f391e416eedf55a67464b5eb88ce2cb2c254cefa0725cdc7367db855eb9f` |
| `src-tauri/target/release/bundle/nsis/Coding Agent Monitor_0.1.0_x64-setup.exe` | `631e7e8511b9fc509ee50e6ec8e9331079cfce9afbebd92cc10682ed321f7b5f` |

These are local verification builds (version string still 0.1.0). The version bump to 0.2.0 and any published-installation checksums happen at the v0.2 release step, after this record is complete.

## 5. Install → launch → uninstall

### NSIS — FULL CYCLE VERIFIED ✅ (2026-08-28)

- **Install:** silent per-user (`/S /currentuser`), exit **0**. Note: NSIS reused the install location remembered from earlier local testing (`<redacted drive>:\DevTools\test\Coding Agent Monitor`) instead of the default `%LOCALAPPDATA%` — an environment artifact of the remembered last-install directory, not a package defect. Installed `coding-agent-monitor.exe` timestamp matched the fresh build.
- **Sidecar integrity:** installed `ccusage.exe` SHA-256 = `3fd2f3b9…245bc` and `ccusage-antigravity.exe` SHA-256 = `f58a7677…5ad42` — both exactly match the staged `src-tauri/binaries/` baselines. ✅
- **Launch:** two instances were observed running simultaneously (one whose origin was not this verification's launch command; both ran the same freshly installed binary). Both had a titled, responding main window; no panic. No `ccusage.exe` process remained after startup collection — the sidecar spawned and was reaped. ✅ (The lack of a single-instance guard is noted as a v0.2+ product observation, not a release blocker.)
- **Uninstall:** silent `uninstall.exe /S`, exit **0** → install directory removed, `HKCU` `Uninstall` registry entry removed, Start Menu shortcut removed, **zero** residual `coding-agent-monitor.exe` / `ccusage.exe` processes. ✅

### MSI — FULL CYCLE VERIFIED ✅ (2026-08-28, elevated)

- **Install (elevated):** `msiexec /i … /qn` via `Start-Process -Verb RunAs` (UAC approved) → exit **0**. When elevated, the MSI registered **per-machine** (`HKLM\…\Uninstall\{326E0D56-F401-4A4B-9AE3-F032A2AEC81E}`) and reused the remembered test install directory (same environment artifact as the NSIS cycle). Installed `coding-agent-monitor.exe` timestamp matched the fresh build; a Start Menu uninstall shortcut was created.
- **Sidecar integrity:** installed `ccusage.exe` = `3fd2f3b9…245bc` and `ccusage-antigravity.exe` = `f58a7677…5ad42` — both exactly match the staged baselines. ✅
- **Launch:** single instance, titled responding main window, correct install path, no panic; no residual `ccusage.exe` after startup collection. ✅
- **Uninstall (elevated):** `msiexec /x {326E0D56-…} /qn` via UAC → exit **0**. Install directory removed, `HKLM` registry entry removed, Start Menu shortcut removed, **zero** residual `coding-agent-monitor.exe` / `ccusage.exe` processes. ✅

## 6. Manual GUI acceptance — RUN (2026-08-28, live app with real data)

Method: the built release binary was launched unpackaged and driven with real mouse/keyboard input; screenshots captured per state/viewport (physical px = logical px × 1.25, display at 125% DPI). Three real agents present (Codex, Claude Code, Antigravity); live usage was actively growing during the pass.

| Item | Result |
|---|---|
| Ready state, real data | **PASS** — 3 dynamic agents rendered; estimated cost correctly hidden (source reports no cost — never faked to $0.00); "~95% cached input" shown |
| 680×700 logical viewport, 3 agents | **FINDING** — the page still shows a vertical scrollbar and the Last-7-Days chart sits below the fold; the plan §6 row ("main content complete, no page scrollbar") is not met at this size with real 3-agent + expanded breakdown content. Not a rendering defect (nothing clipped or overlapping); it is a layout-density gap between the Phase 9 compact query and the matrix target. Disposition: amend the matrix target or tighten compact spacing in a future 0.x — recorded, not blocking |
| 420×560 logical viewport | **PASS** — natural vertical scroll, long model-composition lines wrap gracefully, no horizontal overflow, no clipping (§12 #1 fix confirmed live) |
| Sticky header | **PASS** — Refresh stays in view while scrolled |
| Chart tooltip (mouse hover) | **PASS** — placed beside the bar, inside the viewport; date, total, per-agent color-coded breakdown with shares, and delta all correct |
| Filter chips | **PASS** — Codex chip isolates a single monochrome series, zero day renders "0" with no fake bar (§12 #7), Total recomputes, All restores stacking; day-level Token Breakdown correctly unaffected |
| Agent expandable model detail | **PASS** — all §12.8 notes render with real data: "~92% cached input across 2 models" (token-weighted aggregate), "Agent total includes 1.74K reasoning", real model rows (gpt-5.6-sol, glm-5.3-flash, deepseek-v4-flash, gemini-3.7-flash) |
| Refreshing state | **PASS** — captured live ("Refreshing…", button disabled) when the tray push landed mid-walkthrough |
| Light/dark theme | **PASS** — app follows the system theme live (verified by toggling AppsUseLightTheme and reverting); hierarchy, agent hues, chip contrast readable on both surfaces |
| 200% text scaling | **NOT RUN** — WebView2 in this app does not respond to synthesized zoom input (Ctrl+Plus and Ctrl+wheel both inert), and changing OS display scale mid-session was judged too disruptive; left for an interactive session if desired |
| Keyboard focus-visible + Escape | **PASS** — verified interactively by the user with a real keyboard: focus ring (Indigo) clearly visible across tab stops; Escape closes the tooltip |
| Tray dynamic summary | **PASS** — verified interactively by the user: tray menu shows the "Today: …" summary with top agents; icon hover tooltip correct |

Six-state note: ready / refreshing were captured live. Loading is transient (~1 sidecar round-trip) and empty / first-load error require a data-less or broken environment — these three are covered by the E2E suite (`e2e/dashboard.spec.ts`, fixtures for empty and error paths, 13/13 PASS in §1) and were not reproduced manually here.

## 7. Gate 0 — non-ASCII (Chinese) user-profile pure GUI acceptance

**WAIVED / NOT RUN** — explicitly skipped by the maintainer's decision on 2026-08-28 (same disposition as the v0.1.0 pre-release; a waiver is not a PASS). Binary-level non-ASCII path checks from v0.1 (§3 of `RELEASE_VERIFICATION.md`) remain valid history but do not substitute for this GUI pass. The scenario remains open for a future release.

---

## 8. Residual-process hygiene — PASS (NSIS + MSI)

After both the NSIS and MSI app runs and after both uninstalls, the process list contained **zero** `coding-agent-monitor.exe` or `ccusage.exe` processes.
