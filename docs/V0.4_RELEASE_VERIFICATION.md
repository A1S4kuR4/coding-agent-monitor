# v0.4.0 release verification

Date: 2026-09-10. Final build-source commit: `6b6f9db` (full SHA recorded with the package).

The user explicitly assigned grouped T07/T08 commits, save/check fixes, clean
validation, native acceptance and v0.4.0 packaging. This authorizes local
versioning and packaging; it does not activate quota productization, T09's
Release-page feature or T10 exports. No remote publication or tag is performed.
Historical v0.3 waivers are not new v0.4 technical passes or approved waivers.

## Source and fixes

- `0ba44ce`: T07 history implementation and task evidence.
- `fbc1098`: T08/R1 research and its shared-plan entries, documentation only.
- `214662e`: preference disk errors now reject with safe messages; serialized
  writes preserve old memory/disk on failure and attempt startup rollback;
  language changes take effect after saving; close acknowledgement calls the
  native save-and-hide command and remains visible on error.
- `66e6af1`: ESLint excludes the ignored upstream scratch checkout; strict
  Clippy warnings corrected without disabling warnings; benchmark EOF cleaned.
- `c67b3e8`: product manifests and Cargo lock package version set to 0.4.0.
- `f1df483` / `583935c`: legacy single-flight and worker integration tests now
  use isolated sources instead of the developer's real environment.
- `4e44e52`: timeout/cancellation fault injection uses decimal `60000` rather
  than the unparsable string `60_000`; isolated fast sources exposed this old
  test defect, previously masked by real-data work.
- `6b6f9db`: native acceptance found that the first-close notice remained above
  a scrolled viewport. It now scrolls into view and focuses its first button,
  including repeat close requests.

The T07 commit's whitespace check found a trailing blank line in the new
benchmark; the checks commit fixes it. Historical T07 claims remain dated.
The first added UI failure test exposed a nested alert; the final implementation
uses the enclosing close notice alert and passes the focused regression.

## Clean validation environment

Detached worktree created directly from the build-source commit; no source,
node_modules, dist or Cargo target copied from the development checkout.
`pnpm install --frozen-lockfile` creates a new dependency directory; the existing
pnpm download store, Cargo registry and installed compiler/Windows SDK are
reused. This is a clean source/build validation on the current Windows host,
not a fresh Windows VM or a developer-tools-free host.

Toolchain: Node 20.20.2, pnpm 10.33.0, rustc 1.98.0 stable, Windows 11 Home
10.0.26200 x64 (Chinese host).
Detailed local logs are retained in the ignored `.scaffold-root/v040-evidence/`.

| Check | Current result |
| --- | --- |
| Frozen pnpm install | PASS |
| pnpm lint / typecheck | PASS |
| pnpm test | PASS, 16 files / 143 tests |
| pnpm test:e2e | PASS, 44 tests (browser IPC fixtures) |
| pnpm vendor:verify | PASS |
| pnpm build | PASS |
| cargo fmt -- --check | PASS |
| strict Clippy / Cargo check | PASS; final source Clippy also rerun |
| Cargo test | PASS, 204 tests, 16 result groups including zero doc-tests |
| Tauri MSI/NSIS build | PASS, final clean-worktree source `6b6f9db` |

Initial clean Rust compilation failed with Windows exit `0xc0000043` in
multiple dependency rustc processes. This attempt is FAIL, retained separately;
retesting with `-j 2` does not alter source or relax warning checks.

The first full Rust run was intentionally stopped when inspection revealed a
legacy test reading the real environment. The first isolated rerun exposed two
invalid-delay tests (20 passed, 2 failed in that binary). After fixing their
injection, the complete isolated suite passed: 204 tests, 0 failures. Optional
external sidecar/performance tests that return early remain NOT RUN for those
audits, even though libtest reports their wrapper functions as passed.
A concurrent focused rerun initially hit the running debug EXE's file lock
(Windows error 5); it was not counted as a pass. Final runs use settled binaries.
Rust suite source is `4e44e52`; `6b6f9db` changes only React and its test. Frontend
lint/typecheck/143 unit tests/44 E2E and the Tauri build were rerun for that fix.
The release build retains a nonfatal debug-only Mutex unused-import warning and
a localized MSVC library-creation linker message; strict debug Clippy passes.

## Native acceptance and release decision

**PARTIAL / NOT READY FOR PUBLICATION.** The user requested a status summary
and continuation prompt before the remaining native gates were finished.

### Continuation evidence (2026-09-10 through 2026-09-11)

The final package hashes and signatures were recomputed before this audit and
still match the table below. The release-code baseline is
`99ab422a4488ce5e722fd399a928b82b4db72f02`; later main-branch commits update
acceptance documentation only. The clean detached build checkout remains
exactly `6b6f9db9548ecd3392983e96288079ead8dfe6cf`.

| Check | Result and evidence |
| --- | --- |
| Final MSI administrative extraction | **PASS.** `msiexec /a ... /qn` exited 0 into an isolated evidence directory. The application directory contains exactly `coding-agent-monitor.exe` (15,047,680 bytes, version 0.4.0), `LICENSE` (1,111 bytes), and `THIRD_PARTY_NOTICES.md` (6,031 bytes). There is no ccusage/Antigravity sidecar or other application payload. The two notice-file hashes equal the clean source files. |
| MSI identity and upgrade metadata | **PASS (static/package audit).** ProductName is `Coding Agent Monitor`, ProductVersion is `0.4.0`, ProductCode is `{244735F4-5057-4F83-B4D0-5163FD2FFF74}`, and UpgradeCode is `{4A24D63D-3149-5CC0-97CA-01A4CE86F462}`. The locally retained v0.3 MSI has the same UpgradeCode and a different ProductCode; the v0.4 Upgrade table detects that family. This proves authoring continuity, not an executed upgrade. |
| Final NSIS generated-input audit | **PASS (static/build-input audit).** The exact `target/release/nsis/x64/installer.nsi` generated during the final build identifies version 0.4.0, x64, current-user install mode, and copies only the final NSIS-patched product EXE plus `LICENSE` and `THIRD_PARTY_NOTICES.md`; it declares no external binary. It includes `installer-hooks.nsh`, whose post-install and post-uninstall hooks remove all four historical sidecar names. The installer requests user level for current-user mode and declares PerMonitorV2 DPI awareness for the installer UI. The NSIS binary was not independently unpacked, so this result is deliberately scoped to its exact generated build input. |
| Packaged worker smoke with isolated synthetic data | **PASS.** The EXE extracted from the final MSI and the final NSIS-patched payload source image each processed an explicit seven-day Claude-only synthetic request, returned status `ok` with exactly seven expected date rows, exited 0, wrote no stderr, and left no worker process. No real source was in the request. |
| Final close notice, repeat close, save-failure retry, restored-cache/error UI | **PASS.** Computer Use became available after the host restart. Starting from the bottom of the final candidate's native WebView2 page, both the first and repeated title-bar Close actions moved the notice into the viewport and placed the visible keyboard focus ring on `Got it, don't show again` / `知道了，不再提示`; pressing Tab then moved the ring to the second action. A directory at the isolated `preferences.json.tmp` path caused a real save failure: the UI returned to zh-CN and showed `此偏好设置未能保存。`, while `preferences.json` retained SHA-256 `9f4a1b1804b32e2c1e4d4b609293146aff9bd3bfdc6d71fb7fdde5c5335185fa` and the same timestamp. Removing only that empty synthetic fault directory and repeating the action saved `language: en`, switched the live UI to English, and removed the error. The restored 2026-09-10 synthetic cache, stale timestamp, failed-refresh warning and retained chart were also visibly confirmed. No real usage was displayed. |
| Failed refresh preserves the persisted snapshot file | **PASS.** The final candidate started with the isolated cached snapshot and an intentionally invalid synthetic Claude source. After the refresh attempt, `last-snapshot.json` retained the exact pre-run SHA-256 `26d5b998fa1f3f6cd7feda6670d8c752bea3981308d3eac2179aae507e2ac43e` and timestamp. Computer Use also confirmed the old 2026-09-10 values remained visible with the failed-refresh warning. After a controlled restart without the failure marker, the same final EXE collected the synthetic source successfully, advanced to `Today`, showed `Updated just now`, and a visible Refresh action advanced the collection timestamp again without an error. |
| Final tray language/refresh/open/Exit and worker cancellation | **PASS (human-observed tray UI plus process verification).** Live preference switching proved the main UI changes from Chinese to English. Computer Use could not expose the notification-area popup, so the user completed the tray interaction and supplied an English-menu screenshot showing `Today: 0 · Last success 10:09`, `Refresh now`, `Open dashboard`, and `Exit`. The user reported that `Refresh now` and `Open dashboard` worked, then switched preferences to Chinese and confirmed `立即刷新`, `打开主界面`, and `退出` all worked. After the tray Exit action, an independent process query found zero final-candidate processes and zero `coding-agent-monitor-worker` processes. The zero-token summary is expected: this acceptance instance was deliberately isolated from real usage and the synthetic fixture has no row for 2026-09-11. |
| Final visible/hidden app + WebView2 resource sample | **PASS (measurement completed).** All samples used the exact final EXE with isolated synthetic data. Earlier settled visible samples were app 28.08 MiB plus six WebView2 children 368.09–368.10 MiB, combined 396.16–396.18 MiB. A later visible post-refresh sample was app 30.60 MiB plus six WebView2 children 383.91 MiB, combined 414.51 MiB. At 3 s and 10 s after `Hide now`, the app was 33.68 MiB, the same six WebView2 children totaled 388.07–388.08 MiB, and combined working set was 421.75–421.76 MiB. Every sample had zero worker processes. These are observations, not a broad hardware baseline. |
| Current native scaling environment | **PASS at the current host setting:** `GetDpiForSystem()` reports 96 DPI (100%) and the absent accessibility override resolves to 100% text scaling; the final candidate was visibly inspected at that setting with no clipping in the exercised main, close-notice, error and preferences states. Alternate 125%/150% DPI and 200% text scaling remain **NOT RUN** and require a controlled display/session change. |
| Windows Sandbox / isolated machine availability | **NOT RUN / BLOCKED BY ENVIRONMENT.** `WindowsSandbox.exe` is absent and no isolated VM or machine was provided. The existing v0.3 installation was not used as a disposable upgrade target. No waiver is inferred. |
| Install, upgrade, uninstall lifecycle | **NOT RUN / USER-FROZEN.** On 2026-09-11 the user explicitly froze isolated installation lifecycle work. Administrative extraction is not installation; no installer was executed and no waiver is inferred. |
| Real logon/reboot startup, sleep/resume, offline clean-host run, full non-ASCII profile | **NOT RUN.** These require an appropriate external lifecycle environment; static/offline code and worker evidence do not replace those native gates. No waiver is inferred. |

The two final application EXE hashes differ by bundle type even though their
size and product version match. This is expected for this build: the Tauri log
shows it patches the release EXE first for MSI, packages it, then patches the
same EXE for NSIS and packages it. The MSI-extracted EXE hash is
`44eea56c2ea78658e079fcee1e89cb4d1b30714f8c3ea7b7df96eba64710838c`;
the subsequently NSIS-patched source image hash is
`319d696452a4f9cb549df9a06c56b866aba49574944076afa4f973555e97ff3e`.
This explains the difference without treating either package as stale.

Completed native evidence (isolated synthetic data, release executable):

- GUI starts with a restricted child PATH containing Windows/System32 only;
  no Node/pnpm/Cargo is on that process PATH. This is not a clean-OS claim.
- English dark-theme default view; on-demand thirty-day range has exactly the
  expected 2026-08-12 through 2026-09-10 dates; today's aggregate stays unchanged.
  Selected-day and model/token details are present in the native WebView2 tree.
- Making the isolated preference temp path a directory causes a real disk
  write failure. On the final retry, UI rolled back to zh-CN, showed the save
  error, and left the old file unchanged. Removing the fault and repeating the
  selection saved English and switched the live UI.
- Close acknowledgement persists and hides the window. A second executable
  launch exits with code 0 and restores the one existing main process.
- Found the scrolled close-notice visibility defect and fixed it in `6b6f9db`.
  Native revalidation now confirms viewport movement and first-action focus for
  both the first and repeated Close requests after starting at page bottom.
- Final executable was launched with the prior synthetic snapshot and an
  invalid Claude source. Snapshot file SHA-256 remained identical after the
  failed refresh, and the stale timestamp, failure message and retained values
  were visibly verified. A later no-fault launch and visible Refresh both
  completed successfully against the same synthetic source.
- Synthetic 100,000-line Claude benchmark, 30 fresh worker runs per range:
  seven-day p50/p95 231.92/256.35 ms, peak working set 78.75 MiB; thirty-day
  p50/p95 234.04/247.08 ms, peak 78.73 MiB; both CPU median 218.75 ms.
  Source is 25,977,780 bytes over 60 days; output row counts and diagnostics
  asserted. This is one-source worker evidence, not all-agent native UI memory.

Isolation and cleanup:

- Startup initially failed because the synthetic Windows profile omitted its
  AppData folders (`filesystem_error: unknown path`). Creating isolated
  Roaming/Local directories fixed the harness; this was not a product fix.
- Original application data was backed up before testing and has been restored
  byte-for-byte with SHA-256 checks. The successful native runs use their own
  synthetic HOME/USERPROFILE/AppData/WebView directories. Real autostart
  registration was not changed by the GUI tests.
- Agent-owned native processes were force-stopped for controlled scenario
  changes and cleanup. This does NOT establish graceful tray Exit behavior. The
  previously observed separate executable under
  `D:/DevTools/test/Coding Agent Monitor/` was no longer running and was not
  touched.
- Local test harness, logs, synthetic fixtures and backup hashes remain in
  ignored `.scaffold-root/v040-evidence/`. Never commit those private backups.

Still required / NOT RUN:

1. Alternate 125%/150% DPI, 200% text scaling and minimum-window coverage still
   require controlled native display/session runs; 100% DPI/text scaling is
   now visibly verified on this host.
2. Isolated install/upgrade/uninstall is user-frozen. MSI and NSIS package
   audits are complete; the developer-tools-free/offline clean-host run remains
   NOT RUN. Windows Sandbox is unavailable on this host and no isolated test
   VM/machine has been provided.
3. Real logon/reboot startup, sleep/resume, required DPI/text scaling and full
   non-ASCII Windows profile coverage. No v0.4 waiver has been approved.

## Local delivery directory

**PASS.** A local candidate delivery was assembled at
`D:/DevTools/CodingAgentMonitor-v0.4.0-delivery` with exactly six files: the
MSI, NSIS installer, `SHA256SUMS.txt`, `Release Notes.md`,
`SOURCE_IDENTITY.txt`, and `Acceptance Record.md`. Both installer hashes were
verified from inside that directory against `SHA256SUMS.txt`. The binary source
identity remains `6b6f9db9548ecd3392983e96288079ead8dfe6cf`; documentation-only
acceptance updates do not change that identity. This is a local candidate
handoff, not publication approval. No tag, push, GitHub Release, or upload was
performed.

## Current final-build installers

Build directory: detached clean worktree `D:/DevTools/cam-v040-clean-20260910`.
Full build-source SHA: `6b6f9db9548ecd3392983e96288079ead8dfe6cf`.
Artifacts are under `src-tauri/target/release/bundle/{msi,nsis}` there.
Both Authenticode checks return `NotSigned`. These hashes apply to the final
rebuilt files, not the superseded first candidate.

| File | Bytes | SHA-256 |
| --- | --- | --- |
| `Coding Agent Monitor_0.4.0_x64_en-US.msi` | 5500928 | `4574d0a21fc2fc4633c68be2e4ab4a100ff4b76054374360f4b323b4b4df47e0` |
| `Coding Agent Monitor_0.4.0_x64-setup.exe` | 3601310 | `9831e15d76d0dd464cb470f791a87f97ba85029bd26cff577efcb8e1cc7b3415` |
