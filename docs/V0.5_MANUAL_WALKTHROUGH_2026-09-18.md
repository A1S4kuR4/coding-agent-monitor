# v0.5.0 手动走查清单(2026-09-18)

目的:在 Windows 本机验证 2026-09-18 主界面重构(P0/P1/P2)的全部用户可见行为,并把证据填进
[V0.5_RELEASE_VERIFICATION.md](V0.5_RELEASE_VERIFICATION.md)。走查通过后,在对应条目标 `PASS` 并附截图或一句记录;发现偏差时记录复现步骤即可,不必当场修。

计划环境基线:Windows 11 x64,系统深色模式,默认窗口 680×700,100% DPI。2026-09-23 实际走查宿主为 **125% DPI**（`devicePixelRatio=1.25`），使用已有 release EXE、隔离合成 profile 与 CDP `:9224`；浅色为媒体模拟，未改系统设置。截图与原始驱动日志保存在忽略目录 `.scaffold-root/v05-verify/`，逐项证据见 [验证记录](V0.5_RELEASE_VERIFICATION.md#manual-walkthrough-2026-09-23)。

---

## 第 0 步:Windows 自动检查(命令,非手动)

```powershell
pnpm install
pnpm lint ; pnpm typecheck ; pnpm test          # 预期 16 files / 152 tests 全绿
pnpm test:e2e                                    # 预期 47 passed(系统 Edge)
pnpm vendor:verify ; pnpm build
cargo fmt --manifest-path src-tauri/Cargo.toml -- --check
cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings
cargo test --manifest-path src-tauri/Cargo.toml
```

> 前提:`src-tauri` 的 Claude Desktop 后端(当前未提交)提交后,cargo 三项的结果才覆盖它。

走查用 `pnpm dev`(端口 1420)即可;涉及托盘/生命周期的项等打包后再验。

## A. 首屏(深色,680×700)

| # | 操作 | 预期 | 2026-09-23 结果 |
| --- | --- | --- | --- |
| A1 | 打开主界面,不滚动 | 首屏可见:大数字、delta、单位、meta 行、构成条、agent 列表、"最近 7 天"+ 合计、7天/30天 kicker、筛选 tab 行、一行 mono 说明、7 根柱;无需滚动 | **PASS** · `01` |
| A2 | 观察趋势图 | 今日(末柱)默认选中:浅底衬 + inset 细环,数值全墨加粗;其余柱数值 ink-2(0.72rem),日期刻度 ink-3 | **PASS** · `01`,`10` |
| A3 | 观察今日详情面板 | 图表下方自动展开:完整日期、Token 合计、agent 行(印记+名称+数值·占比)、delta(中性箭头)、成本行、覆盖行、构成明细、每个 agent 的模型折叠块;右上角固定 `✕` | **PASS** · `10`,`21`,`30`；面板内容可纵向滚动 |
| A4 | 构成条默认态 | "按 Agent"激活(kicker 墨色下划线);条为 agent 色分段,总高约 5px;旁边是下划线文本"展开明细" | **PASS** · `01` |
| A5 | 点"按类型" | 条切换为墨色阶梯(缓存读取=others 灰、输入=ink-3、输出=全墨;仅 >0 的类型出现);激活 tab 移到"按类型" | **PASS** · `03` |
| A6 | 点"展开明细"(或点条本体) | 明细行展开:色块 + 名称 + Mono 数值 + 右对齐百分比;按钮文案变"收起明细";再点收起 | **PASS** · `02`,`03` |
| A7 | 观察 agent 行 | 每行下沿一条 3px 细条,宽度≈该 agent 占今日比例,颜色=agent 色 | **PASS** · `01`,`04` |
| A8 | 点面板右上 `✕` | 面板关闭,今日柱取消选中;键盘操作时焦点回到今日柱 | **PASS** · 驱动关闭断言、键盘焦点回归断言 |
| A9 | 再点今日柱 | 重新选中展开(默认选中只生效一次,不自动恢复) | **PASS** · `21` |

## B. 筛选 tab(下划线语言)

| # | 操作 | 预期 | 2026-09-23 结果 |
| --- | --- | --- | --- |
| B1 | 目视筛选行 | 无边框、无底衬、无胶囊;常规态灰字(ink-3),印记在前 | **PASS** · `05` |
| B2 | hover "Codex" | 文字变 ink-2,无任何背景 | **PASS** · hover 色彩探针、`22` |
| B3 | 点击 "Codex" | 文字全墨 + 2px agent 蓝紫下划线;`aria-pressed=true`;上下文提示变为"筛选只影响趋势 — 上方今日摘要始终完整。";上方今日大数字不变 | **PASS** · `22`；125% DPI 下边框计算值 1.6 CSS px = 2 设备 px |
| B4 | 点击 "全部" | 全墨 + 墨色下划线(非 indigo);提示恢复缩放口径"柱高按窗口内最大日缩放;切换筛选会重新缩放。" | **PASS** · 驱动上下文与下划线断言 |
| B5 | 键盘:Tab 走到筛选行 | 每个 tab 可达,焦点环 indigo 2px;Enter/Space 切换;在柱上按 Escape 清除 tooltip 与选中 | **PASS** · `23`、键盘断言 |

## C. 30 天与周聚合

| # | 操作 | 预期 | 2026-09-23 结果 |
| --- | --- | --- | --- |
| C1 | 点 "30 天"(kicker) | 激活下划线为 indigo;标题"最近 30 天";默认**按周**:5 根柱、无横向滚动;出现"按周/按日"第二组 kicker | **PASS** · `08` |
| C2 | 观察状态块 | 原 4 行说明收成一行:`起始 – 结束 · 相对时间`;点击展开:日期范围·时区·包含查询当日、成本、覆盖(以及过期提示,若适用) | **PASS** · 展开探针确认日期、时区、今日、成本；合成数据无覆盖/过期提示条件 |
| C3 | hover 一根周柱 | tooltip 标题为日期范围(如 08/26–09/01);agent 明细完整 | **PASS** · tooltip 日期 `08/25–08/31`，与日详情共享 agent 内容 |
| C4 | 点开一根周柱面板 | 含"所选周 · 当周 Token 与模型明细(全部 Agent)";模型为 7 天合并值;**最后一根(本周)柱无 delta 行** | **PASS** · `24`、周面板与模型折叠块断言；本周无 delta |
| C5 | 点 "按日" | 30 根柱,仅图表区横向滚动,窗口无横向溢出;周↔日切换后选中面板关闭 | **PASS** · `09`、周→日关面板断言 |
| C6 | 点 "7 天" | 回到 7 根柱,今日恢复默认选中;按周/按日组消失 | **PASS** · `10` |
| C7 | C1-C5 期间开启 Codex 筛选 | 周柱变 Codex 单色;筛选对周/日两种聚合一致生效 | **PASS** · `25`、30 根日柱单色断言 |

## D. 成本与负态

| # | 操作 | 预期 | 2026-09-23 结果 |
| --- | --- | --- | --- |
| D1 | 构造缺价日(或用现有缺价数据) | meta 行显示短标记 `成本 n/a ⓘ`(安静、点状下划线);hover tooltip 给出完整原因"预估成本不可用 — 缺少模型价格";**绝不出现 $0.00** | **PASS** · 合成缺价模型、`20` |
| D2 | 无用量日 | 短标记相同,tooltip = "预估成本不适用 — 无用量"(两态文案可区分) | **PASS** · 单独无用量今日启动的 `31`；历史空日完整说明见 `26` |
| D3 | 有价格日 | 正常显示"预估成本 $x.xx",无短标记 | **PASS** · 合成有价日 `$0.19`，驱动断言 |
| D4 | 展开"关于这些数字" | 成本三态、缓存口径(缓存读取 ÷ 输入+缓存读取+缓存创建)文案原样保留 | **FAIL (P2)** · `27`：缓存分母与缺价说明存在，未解释无用量成本态；详见验证记录缺陷条目 |

## E. 健壮性与偏好

| # | 操作 | 预期 | 2026-09-23 结果 |
| --- | --- | --- | --- |
| E1 | 窗口缩到最小(约 420 宽) | 无横向滚动条;header、筛选 tab、kicker 自然换行;柱区内部才允许滚动(仅 30 天按日) | **PASS** · WebView2 420px 视口代理，文档宽 410px，`28`；原生窗口缩放仍待受控会话 |
| E2 | 系统文字缩放 200% | 同上,无横向溢出 | **NOT RUN** · 属 OS 辅助功能设置，留待受控显示会话；未模拟 |
| E3 | 系统开启"减少动态效果" | 下划线/色彩切换瞬时完成;刷新 spinner 只转一次;无动画残留 | **PASS (媒体代理)** · `transition-duration=1e-05s` 即 0.01ms，刷新时 `animation-iteration-count=1` |
| E4 | 切浅色主题 | 印记、下划线、构成条、周柱全部可辨;对比度观感不低于现状 | **PASS (媒体代理)** · `12`,`32`；原生系统切浅色并重启应用 NOT RUN |
| E5 | 点刷新 | 按钮 spinner + 文案"刷新中…";**时间戳旁出现一枚小靛蓝圆点**,刷新结束消失 | **PASS** · 预置 DOM 观察器捕获短暂圆点、禁用按钮和文案，结束后消失；`29` 是结束态截图 |
| E6 | 切到别的窗口再切回(焦点刷新) | 同样的圆点短暂出现(自动刷新可见化) | **NOT RUN** · DOM `focus` 代理未触发 Tauri `onFocusChanged` 路径；需原生切窗 |
| E7 | 中英文切换 | 新词条齐全:按 Agent/按类型、展开/收起明细、按周/按日、成本 n/a ⓘ、关闭详情、较前一周;无中英混排 | **PASS** · `30` 与英文文案探针，完成后切回中文 |

## 记录

- 逐项结果与原始失败尝试已写入 `V0.5_RELEASE_VERIFICATION.md` 的
  "Manual walkthrough (2026-09-23)" 节。唯一产品偏差为 D4（P2）；未在本任务修代码。
- E2、E6、原生浅色重启与其他受控显示会话条件继续保持 NOT RUN，媒体代理不等于原生验证。
