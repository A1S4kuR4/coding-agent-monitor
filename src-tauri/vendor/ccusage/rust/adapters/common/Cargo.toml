[package]
name = "ccusage-adapter-common"
version.workspace = true
edition.workspace = true
publish.workspace = true

[dependencies]
# Downstream (Coding Agent Monitor) 0002 patch: unified read-only source-db
# opening (immutable URI when no live WAL sidecars, bounded busy timeout).
sqlite.workspace = true
ccusage-core.workspace = true
serde.workspace = true
serde_json.workspace = true

[dev-dependencies]
ccusage-test-support.workspace = true
